function mostrar() {
    //Genero el número RANDOM entre 1 y 10 
    var numeroRandom;

    numeroRandom = Math.round(Math.random() * 11); // Me va a salir numeros del 0 al 10

    alert(numeroRandom);

} //FIN DE LA FUNCIÓN